{
  "name": "chat-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@reach/router": "^1.2.1",
    "date-fns": "^1.30.1",
    "firebase": "^9.6.9",
    "node-sass": "^4.11.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "serve": "serve -s build",
    "serve:build": "npm run build && serve -s build",
    "fb:serve": "firebase serve --only functions",
    "init:fb": "firebase init",
    "login:fb": "firebase login",
    "deploy:host": "firebase deploy --only hosting",
    "deploy:fn": "firebase deploy --only functions",
    "deploy": "npm run build && firebase deploy"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "firebase-tools": "^10.4.2",
    "serve": "^13.0.2"
  },
  "main": "src/index.js",
  "repository": "git@github.com:vnscriptkid/chatApp-react-firebase.git",
  "author": "Thanh Nguyen <vnscriptkid@gmail.com>",
  "license": "MIT"
}
